- device_id: sample_roscore #device identifier. It is up to the designer to choose this name.
  className:  embedded.mas.bridges.ros.RosMaster
  microcontroller: 
      id: ros1 
      connectionString: ws://localhost:9090 
      className: DefaultRos4Bdi
  perceptionTopics:         
      # ToDo: configure perception topics
  actions:
    # ToDo: configure actions

# configure perception rules if needed
#- perception_rules:
           
